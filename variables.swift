let userName = "vastavikadi"
print(userName)

let firstName: String
firstName = "Aditya"
print(firstName)

var lastName: String
lastName = "Shukla"
print(lastName)

var secondName = "Shukla"
print(secondName)
// Variables are declared using the var keyword.
// Constants are declared using the let keyword.
// For greater code efficiency, the use of constants instead of variables is recommended whenever possible.
// Type annotation involves specifying the variable type when it is declared. If type annotation is there, then var and const both can be assigned values later in the code, not needed to be done with the declaration
// Type inference allows the compiler to infer the data type based on the value being assigned.

